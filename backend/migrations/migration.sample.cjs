module.exports = {
    /** @param {import('pg').Pool} db */
    up: async (db) => {
        // TODO: make changes
    },

    /** @param {import('pg').Pool} db */
    down: async (db) => {
        // TODO: revert changes
    },
};
